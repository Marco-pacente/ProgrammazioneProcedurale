#include <stdio.h>
#include <stdbool.h>
#define BOOL char //così si fa la stessa cosa
#define TRUE 1
#define FALSE 0
int main(){
    bool booleano = false; //in realtà è un modo per girarci attorno per farlo sembrare più ordinato
    int veroBooleano = 0; //è la stessa cosa
}